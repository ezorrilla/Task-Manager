# Optional: point to a service account JSON file (recommended for local dev)
# Example: C:\path\to\service-account.json or ./firebase-credentials.json
FIREBASE_CREDENTIALS_PATH=./firebase-credentials.json

# Alternative: use Google default credentials
GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\service-account.json

PORT=3000
CORS_ORIGIN=http://localhost:4200
